{
  "name": "romanian-grammar-pocketbook",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "hugo --cleanDestinationDir && node scripts/build-pdf.mjs && node scripts/build-cover.mjs",
    "serve": "hugo server --disableFastRender --baseURL http://localhost:1313/"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer-core": "^24.0.0"
  }
}
